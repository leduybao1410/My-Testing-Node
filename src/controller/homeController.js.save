const getHome = (req, res) => {
	try{
		res.status(200).send("Hello world, my name is Le Duy Bao");
	}catch (err){
		res.status(500).json({ 
		message: "Internal server error",
		error: err.message,
});	}
}

const addString = async (req,res) => {
	try{
	const newString = req.body.string; 
	}
}

module.exports = { getHome,
}
